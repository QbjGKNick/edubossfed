/* eslint-disable camelcase */
export interface IUserInfo {
  access_token: string
  expires_in: number
  jti: string
  organization: string
  refresh_token: string
  scope: string
  token_type: string
  user_id: string
}
